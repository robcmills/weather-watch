import Ember from 'ember';

export default Ember.Route.extend({
  // model: function(params) {
  //   console.log('weather model params', params);
  //   return Ember.$.getJSON(this.controllerFor('weather').get('apiUrl'));
  // }
});
